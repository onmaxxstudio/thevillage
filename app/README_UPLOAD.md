# Corrected GitHub Blueprint Package

This package fixes the earlier image-name mismatch.

The correct signup/sign-in reference is:

`design/02-create-account-and-sign-in.png`

Upload the contents of this folder to the root of The Village GitHub
repository.

Do not upload the ZIP itself. Extract it first, then upload: -
`THE_VILLAGE_BLUEPRINT.md` - the entire `design/` folder

If older incorrectly named design files are already in GitHub, remove or
replace them to avoid confusing Codex.
